#include <stdio.h>
#include <stdint.h>
void PrintMAC(char* msg, uint8_t *mac);
int CountLine(char* file_name);
void ReadFile(char* file_name, char** line);
char* EraseEnter(char* str);
void text_align_center(int columns, char* text);
int GetStringLen(char* str);
